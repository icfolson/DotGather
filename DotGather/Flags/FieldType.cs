namespace DotGather.Flags
{
    /// <summary>
    /// Custom ENUM of the different types of GatherContent Fields.
    /// </summary>
    public enum FieldType
    {
        text,
        choice_radio,
        files,
        section,
        choice_checkbox,
    }
}
